[[checks]]
  matchPattern = "Automatically determined partition size .* is smaller than default .*, using default."
  dataFile = "run_%d.0.log"
  numMatches = 1
  testType = "FILE_MATCH_PATTERN"
  
[run]
  mdlfiles = ["Scene.main.mdl"]

