begin model

begin parameters
    a 3.5
    b 2
    c a+b
    d a+b*c
    e (a+b)*c 
    f a-b*c
    g (a-b)*c
    h a^b*c
    i a^(b*c)
    j a*b/d
    l (a*b)/d
    k a+b*(c-d/e^f)
    m -a
    n +a
    o a^(b*c)-d/e*f+g
    p a^-5
    q -2^+2
end parameters

end model
