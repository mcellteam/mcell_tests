includes = ["exit_code_1"]

[[checks]]
  matchPattern = "Unexpected empty string as value of Release site - quantity"   
  dataFile = "err_%d.0.log"
  numMatches = 1
  testType = "FILE_MATCH_PATTERN"

[[checks]]
  testType = "CHECK_EXIT_CODE"
  exitCode = 1
  
[run]
  jsonFile = "data_model.json"
  bnglMode = true
